# =============================================================================
# QUALITY GATES: {{squad_name}}
# =============================================================================
# Generated by @squad-creator (Craft)
# Extends global quality gates from operabase.yaml

extends: global
squad: "{{squad_name}}"

# Additional gates specific to this squad
additional_gates:
  pre_commit:
    - name: "{{gate_name}}"
      command: "{{gate_command}}"
      description: "{{gate_description}}"

# Override global gates (optional)
# overrides:
#   pre_commit:
#     - name: lint
#       command: "custom lint command"
